# array와 2차원 배열 command [l,m,n]을 입력받아
# array의 l번째 원소부터 m번째 원소까지 리스트를 추출 후 정렬시켜
# n번째 원소를 반환하는 함수

def solution(array, commands):

    answer = []
    print(len(commands))
    for i in range(len(commands)):
        a = commands[i][0]
        b = commands[i][1]
        c = commands[i][2]
        l = sorted(array[a-1:b])
        answer.append(l[c-1])


    return answer
